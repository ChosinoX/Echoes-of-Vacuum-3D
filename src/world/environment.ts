import * as THREE from'three';
export function createDust(){const count=900,g=new THREE.BufferGeometry(),p=new Float32Array(count*3);for(let i=0;i<count;i++){p[i*3]=(Math.random()-.5)*145;p[i*3+1]=.15+Math.random()*12;p[i*3+2]=(Math.random()-.5)*145}g.setAttribute('position',new THREE.BufferAttribute(p,3));const points=new THREE.Points(g,new THREE.PointsMaterial({color:0xd39a72,size:.055,transparent:true,opacity:.28,depthWrite:false}));points.userData.base=p.slice();return points}
export function updateDust(dust:THREE.Points,t:number){dust.rotation.y=t*.003;const a=dust.geometry.attributes.position as THREE.BufferAttribute;for(let i=0;i<a.count;i++)a.setX(i,a.getX(i)+.0025);a.needsUpdate=true}
export function addHorizon(scene:THREE.Scene){const mat=new THREE.MeshStandardMaterial({color:0x120d0d,roughness:1,side:THREE.DoubleSide});for(let i=0;i<24;i++){const a=i/24*Math.PI*2,r=72+8*Math.sin(i*2.1),h=7+8*Math.abs(Math.sin(i*1.73));const g=new THREE.ConeGeometry(8+(i%4)*3,h,5);const m=new THREE.Mesh(g,mat);m.position.set(Math.cos(a)*r,h/2-3,Math.sin(a)*r);m.rotation.y=-a;m.receiveShadow=true;scene.add(m)}}
