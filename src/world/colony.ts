import * as THREE from'three';import{attachGLB}from'../core/assets';import{terrainHeight}from'./terrain';
function place(scene:THREE.Scene,url:string,h:number,x:number,z:number,rot=0){const g=new THREE.Group();g.position.set(x,terrainHeight(x,z),z);g.rotation.y=rot;scene.add(g);void attachGLB(g,url,h);return g}
function corridor(scene:THREE.Scene,a:[number,number],b:[number,number]){const dx=b[0]-a[0],dz=b[1]-a[1],len=Math.hypot(dx,dz),steps=Math.max(1,Math.floor(len/2.8));for(let i=1;i<steps;i++){const t=i/steps,x=a[0]+dx*t,z=a[1]+dz*t;place(scene,'/models/kenney/corridor_detailed.glb',1.35,x,z,Math.atan2(dx,dz))}}
export function addColonySetDressing(scene:THREE.Scene){const hub:[number,number]=[0,0];for(const p of [[-8,-3],[6,-6],[-7,7],[10,8],[0,9]]as[number,number][])corridor(scene,hub,p);place(scene,'/models/kenney/barrels.glb',1.15,3.4,8.2,.4);place(scene,'/models/kenney/barrels.glb',1.15,-3.2,8.6,-.5);place(scene,'/models/kenney/craterLarge.glb',.8,22,-17,.2);place(scene,'/models/kenney/craterLarge.glb',.65,-29,25,-.4)}
