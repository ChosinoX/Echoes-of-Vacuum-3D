import * as THREE from'three';import{terrainHeight}from'./terrain';
export function addColonyLights(scene:THREE.Scene){const fixtures:[[number,number],[number,number]][]=[[[0,0],[-8,-3]],[[0,0],[6,-6]],[[0,0],[-7,7]],[[0,0],[10,8]],[[0,0],[0,9]]];const lights:THREE.PointLight[]=[];for(const [a,b]of fixtures){const steps=Math.max(2,Math.floor(Math.hypot(b[0]-a[0],b[1]-a[1])/3));for(let i=1;i<steps;i++){const t=i/steps,x=THREE.MathUtils.lerp(a[0],b[0],t),z=THREE.MathUtils.lerp(a[1],b[1],t),y=terrainHeight(x,z);const pole=new THREE.Mesh(new THREE.CylinderGeometry(.035,.05,.65,6),new THREE.MeshStandardMaterial({color:0x343b3d,metalness:.8,roughness:.4}));pole.position.set(x,y+.32,z);scene.add(pole);const bulb=new THREE.Mesh(new THREE.SphereGeometry(.07,8,6),new THREE.MeshStandardMaterial({color:0xffc27a,emissive:0xff8a38,emissiveIntensity:3}));bulb.position.set(x,y+.68,z);scene.add(bulb)}}for(const [x,z]of [[-3,-2],[3,-2],[-4,5],[4,5]]as[number,number][]){const l=new THREE.PointLight(0xff9b55,0,11,2);l.position.set(x,terrainHeight(x,z)+2,z);scene.add(l);lights.push(l)}return lights}
export function updateColonyLights(lights:THREE.PointLight[],night:number){for(const l of lights)l.intensity=night*7}
