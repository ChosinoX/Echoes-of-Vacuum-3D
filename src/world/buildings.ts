import * as THREE from'three';import type{BuildingData,BuildingType}from'../types';import{terrainHeight}from'./terrain';import{attachGLB}from'../core/assets';
const model:Record<BuildingType,{url:string,h:number}>={command:{url:'/models/kenney/structure_detailed.glb',h:5.1},solar:{url:'/models/kenney/machine_generatorLarge.glb',h:2.8},oxygen:{url:'/models/kenney/machine_generator.glb',h:3.1},hydroponics:{url:'/models/kenney/structure_closed.glb',h:3.8},mining:{url:'/models/kenney/craft_miner.glb',h:3.3},storage:{url:'/models/kenney/craft_cargoA.glb',h:3.1}};
function ghostGeometry(type:BuildingType){const w=type==='command'?5.5:type==='mining'?5:4,d=type==='hydroponics'?5:4,g=new THREE.BoxGeometry(w,1.1,d);g.translate(0,.55,0);return g}
export function createBuilding(data:BuildingData,ghost=false){const group=new THREE.Group();group.userData={building:data};if(ghost)group.add(new THREE.Mesh(ghostGeometry(data.type),new THREE.MeshStandardMaterial({color:0x5ba58d,transparent:true,opacity:.28,depthWrite:false})));else{const m=model[data.type];void attachGLB(group,m.url,m.h);if(data.type==='command'){const dish=new THREE.Group();dish.position.set(2.2,.1,-1.5);group.add(dish);void attachGLB(dish,'/models/kenney/satelliteDish_detailed.glb',2.2)}}group.position.set(...data.position);group.rotation.y=data.rotation;return group}
export function makeBuilding(type:BuildingType,x:number,z:number,progress=1):BuildingData{return{id:crypto.randomUUID(),type,position:[x,terrainHeight(x,z),z],rotation:0,progress,materialsDelivered:progress>=1}}
