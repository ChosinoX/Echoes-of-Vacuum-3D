import * as THREE from 'three';
export class StrategyCamera{camera=new THREE.PerspectiveCamera(48,innerWidth/innerHeight,.1,300);target=new THREE.Vector3();yaw=.72;pitch=.78;distance=38;drag=false;last=new THREE.Vector2();constructor(private dom:HTMLElement){this.bind();this.update()}private bind(){this.dom.addEventListener('wheel',e=>{e.preventDefault();this.distance=THREE.MathUtils.clamp(this.distance+e.deltaY*.025,13,70)},{passive:false});this.dom.addEventListener('pointerdown',e=>{if(e.button===1||e.button===2){this.drag=true;this.last.set(e.clientX,e.clientY);this.dom.setPointerCapture(e.pointerId)}});this.dom.addEventListener('pointerup',()=>this.drag=false);this.dom.addEventListener('contextmenu',e=>e.preventDefault());this.dom.addEventListener('pointermove',e=>{if(!this.drag)return;const dx=e.clientX-this.last.x,dy=e.clientY-this.last.y;this.last.set(e.clientX,e.clientY);if(e.buttons===2){this.yaw-=dx*.006;this.pitch=THREE.MathUtils.clamp(this.pitch+dy*.005,.3,1.35)}else{const right=new THREE.Vector3(Math.cos(this.yaw),0,-Math.sin(this.yaw));const forward=new THREE.Vector3(Math.sin(this.yaw),0,Math.cos(this.yaw));this.target.addScaledVector(right,-dx*.035).addScaledVector(forward,-dy*.035)}})}update(){this.camera.position.set(this.target.x+Math.sin(this.yaw)*Math.cos(this.pitch)*this.distance,this.target.y+Math.sin(this.pitch)*this.distance,this.target.z+Math.cos(this.yaw)*Math.cos(this.pitch)*this.distance);this.camera.lookAt(this.target)}}
