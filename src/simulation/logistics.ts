import * as THREE from'three';import type{BuildingData}from'../types';import type{Mover}from'./pathfinding';export type DeliveryState='idle'|'to-site'|'unloading'|'returning';export interface LogisticsRover{mover:Mover;object:THREE.Group;deliveryState:DeliveryState;deliveryId:string|null;timer:number}export function initLogisticsRover(r:any){return Object.assign(r,{deliveryState:'idle' as DeliveryState,deliveryId:null,timer:0})}export function updateLogistics(r:LogisticsRover,buildings:BuildingData[],dt:number){let site=r.deliveryId?buildings.find(b=>b.id===r.deliveryId):undefined;if(r.deliveryState==='idle'){site=buildings.find(b=>b.progress<1&&!b.materialsDelivered);if(site){r.deliveryId=site.id;r.deliveryState='to-site';r.mover.setTarget(new THREE.Vector3(site.position[0]+2.6,site.position[1],site.position[2]))}}else if(r.deliveryState==='to-site'&&!r.mover.target){r.deliveryState='unloading';r.timer=2.5}else if(r.deliveryState==='unloading'){r.timer-=dt;if(r.timer<=0&&site){site.materialsDelivered=true;r.deliveryState='returning';r.mover.setTarget(new THREE.Vector3(7,0,5))}}else if(r.deliveryState==='returning'&&!r.mover.target){r.deliveryState='idle';r.deliveryId=null}}